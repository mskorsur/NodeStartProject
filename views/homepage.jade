doctype html
html
  head
    meta(charset="utf-8")
    title Example program
  body
    h2 #{title}
    h4 #{message}
    p To proceed choose one of the following:
    ul
      li /data to get items from database, 
           a(href="/data") Get data
      li /send to send items to database,
           a(href="/send") Send data